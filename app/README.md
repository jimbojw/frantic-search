# Frantic Search — App

The main SolidJS single-page application for Frantic Search. Mobile-first, built with Vite.

This app will host the WebWorker that performs instant search across MTG cards without blocking the UI.

## Development

From the project root:

```bash
npm run dev
```

Then open [http://localhost:5173](http://localhost:5173).
