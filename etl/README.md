# Frantic Search — ETL

The Extract-Transform-Load pipeline for Frantic Search. Runs in Node.js.

Fetches raw MTG card data (e.g., from Scryfall bulk data) and transforms it into an optimized JSON format for the client app.
