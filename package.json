{
  "name": "frantic-search",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w app",
    "cli": "npm run -s run -w cli --",
    "etl": "npm run -s run -w etl --",
    "generate-icons": "./scripts/generate-icons.sh",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "setup": "npm install && npm run etl -- download && npm run etl -- download-tags && npm run etl -- download-mtgjson && npm run etl -- process",
    "test": "npm run typecheck && npm test --workspaces --if-present && npm run cli -- compliance"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "repository": "github:jimbojw/frantic-search",
  "bugs": "https://github.com/jimbojw/frantic-search/issues",
  "homepage": "https://franticsearch.gg/",
  "description": "Instant search across Magic: The Gathering cards",
  "workspaces": ["app", "cli", "etl", "shared"]
}
