# Frantic Search — Shared

Shared TypeScript library for Frantic Search.

Defines common types (`Card`, `Set`) and the core search/filter logic used by both the app (including the WebWorker) and the ETL scripts.
